---
- hosts: localhost
  tasks:
    - name: Uninstall 'sl' command for Debian
      apt: name=sl state=absent
      become: yes
      when: ansible_os_family == 'Debian'

    - name: Uninstall 'sl' command for Redhat
      yum: name=sl state=absent
      become: yes
      when: ansible_os_family == 'RedHat'
      
